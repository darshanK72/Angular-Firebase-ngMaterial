import { YellowbgDirective } from './yellowbg.directive';

describe('YellowbgDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowbgDirective();
    expect(directive).toBeTruthy();
  });
});
